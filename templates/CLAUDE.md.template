# CLAUDE.md — [PROJECT_NAME]

<!--
  This file is the AI agents' "brain": it is read by Claude (and the PGE workflows
  — decompose / plan / implement / code-review / evaluate) before writing or
  reviewing any code. Keep it accurate and up to date as the project evolves.

  HOW TO FILL THIS IN
  1. Replace every [PLACEHOLDER] with your project's real content.
  2. Delete sections / rows that do not apply (this template is tech-stack neutral —
     it suits Android/Kotlin, Node/TS, Python, Go, monorepo or single module).
  3. The `<!-- e.g. ... -->` comments are examples only — delete them after filling in.
  4. The more concrete and specific you are, the better the AI agents perform.
-->

---

## Project Overview

[PROJECT_NAME] is a [one-paragraph description: what it does and who it is for].

<!-- e.g. "Telenav Search Android Demo — a sample app showing how to integrate the
     Vivid Entity SDK. Developer reference, not a production product." -->

Key components / services:
- **[component-1]** — [responsibility]
- **[component-2]** — [responsibility]

<!-- For a monorepo, list each package; for a single module, list the main areas. -->

---

## Tech Stack

| Category | Technology / Version |
|----------|----------------------|
| Language | [e.g. Kotlin 1.9 / TypeScript 5 / Python 3.12] |
| Build tool | [e.g. Gradle / Vite / Poetry] |
| Framework | [e.g. Android Views + Data Binding / Next.js / FastAPI] |
| Key libraries | [list the libraries the AI is expected to use] |
| Package manager | [e.g. Gradle / yarn / pip] |

**Not used in this project:** [list explicitly excluded tech so the AI never reaches for it].
<!-- e.g. "Hilt/Dagger, Retrofit, Jetpack Compose, Room" — naming exclusions prevents
     the agent from introducing dependencies you have deliberately avoided. -->

---

## Project / Module Structure

```
[repo-root]/
├── [dir-1]/        # [what lives here]
├── [dir-2]/        # [what lives here]
└── [build/config files]
```
<!-- A short directory tree with one-line annotations. Point out where source,
     tests, and config/secrets live. -->

---

## Configuration & Secrets

Secrets are injected via [mechanism]; they are **never** committed or hardcoded.

<!-- e.g. "via gradle.properties (gitignored), exposed as BuildConfig fields" or
     "via environment variables loaded from a secret manager". -->

Required configuration:
- `[CONFIG_KEY_1]` — [purpose]
- `[CONFIG_KEY_2]` — [purpose]

> **Never hardcode** API keys, credentials, endpoints, or PII in source files.

---

## Build & Verification Commands

```bash
[install / bootstrap command]      # e.g. ./gradlew dependencies | yarn install
[build command]                    # e.g. ./gradlew assembleDebug | yarn build
[lint command]                     # e.g. ./gradlew lint | yarn lint
[test command]                     # e.g. ./gradlew test | yarn test
```

After making changes, always run **[the build + test command]** to confirm a clean build.

> These commands should match what `.github/scripts/pge-verify.sh` runs, so the
> Evaluator verifies the PR the same way you do locally.

---

## Architecture & Coding Conventions

### Architecture Pattern
[Describe the high-level pattern, or state that none is strictly enforced.]
<!-- e.g. "Single-module app; Activities talk directly to SDK clients" or
     "Layered: UI → BFF → API client; reverse imports forbidden". -->

### Naming Conventions
[Directory / package / file / symbol naming rules specific to this project.]

### Key Patterns & Hard Rules
1. [Rule 1 — a concrete, checkable constraint]
2. [Rule 2]
3. [Rule 3]
<!-- e.g. "Never use GlobalScope; use lifecycleScope/viewModelScope",
     "All external API calls go through the BFF layer". -->

---

## Code Review Focus Areas

When reviewing PRs (code-review / Evaluator), pay special attention to:

1. **[Focus area 1]** — [what to check and why]
2. **[Focus area 2]** — [what to check and why]
3. **[Focus area 3]** — [what to check and why]
<!-- e.g. "Secrets exposure", "SDK engine lifecycle balance",
     "Coroutine scope misuse", "Null-safety / unchecked !!". -->

---

## Domain SKILL Files

<!-- SKILL files the AI agents should read. Cross-repo skills are copied from the hub
     into .cursor/skills/; add your own project-specific skills below. -->

### Cross-repo Skills (baseline — always read on every PR)

| Skill | Path |
|-------|------|
| Clean Code (baseline) | `.cursor/skills/clean-code/SKILL.md` |
| Refactor (baseline) | `.cursor/skills/refactor/SKILL.md` |

### Domain Skills (read when the change touches that area)

| Skill | Path | Use When |
|-------|------|----------|
| Frontend | `.cursor/skills/frontend/SKILL.md` | UI / client code: components, state, styling, forms, a11y |
| Backend | `.cursor/skills/backend/SKILL.md` | Server code: controllers, services, data access, jobs |
| API Design | `.cursor/skills/api-design/SKILL.md` | Adding/changing an endpoint, request/response shape, error format |
| Testing | `.cursor/skills/testing/SKILL.md` | Any PR that adds or changes behavior |
| Security | `.cursor/skills/security/SKILL.md` | Untrusted input, auth, secrets, crypto, uploads, dependencies |

### Project-specific Skills

| Skill | Path | Use When |
|-------|------|----------|
| [Development Standard] | `[path]/.cursor/skills/development-standard/SKILL.md` | [Any task in this area] |
<!-- Add more as you create them. -->

---

## AI Workflow Stage Guidance

These notes guide the PGE stages. The Tech Stack and Conventions above apply to every stage.

### Decompose
[How to split a large issue into sub-issues for this project.]
<!-- e.g. "Split by feature area / service; each sub-issue must be independently
     buildable and testable with [build command]." -->

### Plan (Milestones)
[How to slice milestones so each is independently shippable.]
<!-- e.g. "Split by feature area rather than architecture layer; keep each milestone
     independently buildable." -->

### Implement / Code Review / Evaluate
Follow **Architecture & Coding Conventions** and **Code Review Focus Areas** above.
Hard rules to enforce: [restate the few non-negotiable rules].
Build verification: **[build + test command]**.

---

## PGE Issue Labeling

Every issue must have all three required label categories:

| Category | Labels |
|----------|--------|
| **Type** | `pge/type:prd` or `pge/type:change` |
| **Scope** | `pge/scope:[your-scope]` — add all affected scopes |
| **Status** | `pge/status:triage` (always start here) |

### Available scope labels
<!-- Update these to match your project's scope labels in .github/labels.yml -->
- `pge/scope:[scope-1]`
- `pge/scope:[scope-2]`

---

## What NOT to Add Without Discussion

List technologies / patterns that require an explicit decision before introducing them,
so the AI does not silently pull them in:

- **[Tech / pattern 1]** — [why it needs discussion]
- **[Tech / pattern 2]** — [why it needs discussion]
<!-- e.g. "Hilt/Dagger — no DI framework today; adding one is a big refactor",
     "New SDK dependencies — confirm registry availability first". -->
